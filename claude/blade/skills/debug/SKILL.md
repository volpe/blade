---
name: debug
description: >
  Debug with hypothesis → evidence → root cause → fix. Use for /blade-debug,
  failing tests, regressions, or “why is this broken”.
argument-hint: "[symptom | ticket | failing test]"
disable-model-invocation: true
---

Read [the blade-debug playbook](../../playbooks/blade-debug/SKILL.md) completely and follow it for the request below. Resolve its relative links from the playbook's directory. Read the file directly; do not invoke another slash command. Use the current conversation when the request is empty.

Request:
$ARGUMENTS
